<DataTemplate 
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
	xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
	xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
	>
	<Grid
		x:Name="RootGrid"
		MinWidth="420"
		Margin="6 8 6 8"
		ShowGridLines="False"
		Background="#202030"
		>
		<Grid.Resources>
			<Style TargetType="ListView" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogListViewStyleKey}}" />
			<Style TargetType="Button" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}" />
			<Style TargetType="TextBlock">
				<Setter Property="Foreground" Value="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}" />
			</Style>
		</Grid.Resources>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="100" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="40" /> <!-- Prompt and Button-->
			<RowDefinition Height="40" /> <!-- Path -->
			<RowDefinition Height="40" /> <!-- Prompt -->
			<RowDefinition Height="40" /> <!-- Combo -->
			<RowDefinition Height="40" /> <!-- Prompt -->
			<RowDefinition Height="40" /> <!-- Text Box for name  -->
		</Grid.RowDefinitions>
		<TextBlock   
			Grid.Row="0" Grid.Column="0"
			Margin="4 0 0 0"
			Text="Selected Folder: " 
			VerticalAlignment="Center"
			/>
		<Button
			Grid.Row="0" Grid.Column="1"
			Height="28"
			Margin="4"
			Content="Change..."
			>
			<vs:ExtensibilityUICommands.EventHandlers>
				<vs:EventHandlerCollection>
					<vs:EventHandler 
						Event="Click"
						Command="{Binding DataContext.ChangeFolderCommand, ElementName=RootGrid}"
						CommandParameter="{Binding}"
						CounterTarget="{Binding RelativeSource={RelativeSource Self}}" 
						/>
				</vs:EventHandlerCollection>
			</vs:ExtensibilityUICommands.EventHandlers>
		</Button>
		<TextBox   
			Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
			Margin="12 0 0 0"
			Height="28"
			Text="{Binding SelectedFolderText}"
			VerticalAlignment="Center"
			/>
		<TextBlock   
			Grid.Row="2" Grid.Column="0"
			Margin="4 0 0 0"
			Text="Select a Template: " 
			VerticalAlignment="Center"
			/>
		<ComboBox
			x:Name="TemplatesComboBox"
			Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
			Margin="12 4 0 4" Height="32"
			VerticalAlignment="Center"
			ItemsSource="{Binding Names}"
			SelectedItem="{Binding SelectedItemKind}"
			>
			<!--
			<vs:EventHandlerCollection>
				<vs:EventHandler
					Event="SelectionChanged"
					Command="{Binding DataContext.SelectionChangedCommand, ElementName=RootGrid}"
					CommandParameter="{Binding SelectedItem, ElementName=TemplatesComboBox}"
					CounterTarget="{Binding RelativeSource={RelativeSource Self}}"
					/>
			</vs:EventHandlerCollection>

			-->
		</ComboBox>
		<TextBlock   
			Grid.Row="4" Grid.Column="0" 
			Margin="4 0 0 0"
			Text="Item Name: " 
			VerticalAlignment="Center"
			/>
		<TextBox
			x:Name="ItemNameTextBox"
			Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
			Margin="12 0 0 0"
			Height="32"
			VerticalAlignment="Center"
			VerticalContentAlignment="Center"
			Text="{Binding SelectedItemName}"
			>
			<!--
			<vs:EventHandlerCollection>
				<vs:EventHandler
					Event="TextChanged"
					Command="{Binding DataContext.TextChangedCommand, ElementName=RootGrid}"
					CommandParameter="{Binding}"
					CounterTarget="{Binding RelativeSource={RelativeSource Self}}"
					/>
			</vs:EventHandlerCollection>
			-->
		</TextBox>
	</Grid>
</DataTemplate>
